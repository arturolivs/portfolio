const SM = 768
const MD = 1024
const LG = 1440
const XL = 2200

export default {
  minSM: `min-width: ${SM}px`,
  minMD: `min-width: ${MD}px`,
  minLG: `min-width: ${LG}px`,
  minXL: `min-width: ${XL}px`,
}
